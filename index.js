/**
 * Recall App - Entry Point
 * 
 * Local-first semantic search for images and documents
 */

import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('main', () => App);
